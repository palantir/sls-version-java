type: improvement
improvement:
  description: Parsing an `OrderableSlsVersion` no longer runs the regex matcher twice,
    reducing allocation slightly
  links:
  - https://github.com/palantir/sls-version-java/pull/462
